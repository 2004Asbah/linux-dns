# 🧪 Caching-Only DNS Lab (BIND 9) - RHEL/Rocky

This document outlines the steps followed to set up a caching-only DNS server using BIND.

---

## ✅ Step 1: Install Required Packages

```bash
sudo dnf install bind bind-utils -y
